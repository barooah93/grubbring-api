//TODO: fix

/*var app = angular.module('grubbring.orderServices', []);

app.factory('OrderService', ['$http', 'activityId', function($http, activityId) {
    return {
        getNumOpenOrders: function(activityId) {
            return $http.get("/api/orders/numOpenOrders/" + activityId)
        }
    };
}]);*/